jQuery( document ).ready( function( $ ) {

	$( '#dedo-select-download-dropdown' ).chosen( { disable_search_threshold: 10, width: "100%" } );
	$( '#dedo-select-style-dropdown' ).chosen( { disable_search_threshold: 10, width: "25%" } );
	$( '#dedo-select-button-dropdown' ).chosen( { disable_search_threshold: 10, width: "25%" } );

} );